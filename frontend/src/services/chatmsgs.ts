export class Chatmsgs {
    
    reply: string;
    image: string;
    id: string;
    constructor(reply: string, image: string, id: string) {

        this.reply = reply;
        this.image = image;
        this.id = id;
    }
}